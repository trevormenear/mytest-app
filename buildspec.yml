version: 0.2

env:
  variables:
    IMAGE_REPO_NAME: mytest-app

phases:
  pre_build:
    commands:
      - echo "Logging in to Amazon ECR..."
      - ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
      - REGION=${AWS_DEFAULT_REGION:-us-east-1}
      # Make a unique tag: <shortsha>-b<buildnum>-<epoch>
      - SHORT_SHA=$(echo "$CODEBUILD_RESOLVED_SOURCE_VERSION" | cut -c1-12)
      - BUILD_NUM=${CODEBUILD_BUILD_NUMBER:-0}
      - EPOCH=$(date +%s)
      - IMAGE_TAG="${SHORT_SHA}-b${BUILD_NUM}-${EPOCH}"
      - REPO_URI="$ACCOUNT_ID.dkr.ecr.$REGION.amazonaws.com/$IMAGE_REPO_NAME"
      - echo "Using image tag: $IMAGE_TAG"
      - aws ecr get-login-password --region "$REGION" | docker login --username AWS --password-stdin "$REPO_URI"
  build:
    commands:
      - echo "Build started on $(date)"
      - docker build -t "$IMAGE_REPO_NAME" .
      - docker tag "$IMAGE_REPO_NAME:latest" "$REPO_URI:$IMAGE_TAG"
  post_build:
    commands:
      - echo "Pushing the Docker image..."
      - docker push "$REPO_URI:$IMAGE_TAG"
      - echo "IMAGE_URI=$REPO_URI:$IMAGE_TAG"
      - printf '{"imageUri":"%s"}' "$REPO_URI:$IMAGE_TAG" > imageDetail.json

artifacts:
  files:
    - imageDetail.json
